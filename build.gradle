buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.5.0'
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.2.1'
}